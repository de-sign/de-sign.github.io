{
  "name": "de-sign.github.io",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.3.9"
  }
}
